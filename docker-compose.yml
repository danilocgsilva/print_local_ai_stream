services:
  print_stream_local_ollama:
    build:
      context: .
    container_name: print_stream_local_ollama
    volumes:
      - './app:/app'
    working_dir: /app
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8099:8099"
